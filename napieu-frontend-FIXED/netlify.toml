[build]
  command = "npm run build"
  publish = ".next"  # Changed from "out" - uses Next.js build output

[build.environment]
  NODE_VERSION = "18"
  NEXT_PUBLIC_API_URL = "https://muhammadfaizan-practice-7.onrender.com"

# Use Netlify's Next.js plugin for proper SSR/dynamic routes
[[plugins]]
  package = "@netlify/plugin-nextjs"